# Codex project instructions

- Read and follow `CLAUDE.md` for the project architecture and engineering rules.
- When executing an implementation plan, create and use an isolated git
  worktree by default. The user has granted persistent consent for Codex CLI
  sessions in this repository; do not ask again unless worktree creation would
  require a destructive action or conflict with an explicit instruction.
- Preserve unrelated user changes in the primary checkout.
